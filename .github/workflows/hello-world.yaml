on:
  push:
    branches:
      - master  # Or whichever branch you want

jobs:
  hello:
    name: Say Hello shruti 
    runs-on: [self-hosted , webserver ]

    steps:
      - name: Print Hello World shruti
        run: echo "Hello, World from self-hosted runner!"
